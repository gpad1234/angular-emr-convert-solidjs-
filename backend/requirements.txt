# ============================================================
# Diabetes EMR - Backend Python Dependencies
# ============================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# For a virtual environment (recommended):
#   python -m venv venv
#   source venv/bin/activate        (macOS/Linux)
#   venv\Scripts\activate.bat       (Windows)
#   pip install -r requirements.txt
# ============================================================

# Core web framework - provides automatic OpenAPI docs, request validation,
# async support, and dependency injection
fastapi==0.111.0

# ASGI server that runs the FastAPI app
# [standard] includes websockets and watchfiles for --reload support
uvicorn[standard]==0.29.0

# SQLAlchemy ORM - maps Python classes to database tables
# Handles connection pooling, transactions, and query building
sqlalchemy==2.0.30

# Pydantic v2 - data validation and serialization
# FastAPI uses this for request/response models
# [email] adds email validation support
pydantic[email]==2.7.1

# Date utilities - for parsing and formatting dates in sample data
python-dateutil==2.9.0.post0
